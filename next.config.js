/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "dummyimage.com", "wcs.smartdraw.com"],
  },
};

module.exports = nextConfig;
